---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: rulechecks.caelus.io
spec:
  group: caelus.io.
  names:
    kind: RuleCheck
    listKind: RuleCheckList
    plural: rulechecks
    singular: rulecheck
  scope: Namespaced
  versions:
    - name: v1
      schema:
        openAPIV3Schema:
          description: RuleCheck is the Schema for the rulechecks API
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: RuleCheckSpec defines the desired state of RuleCheck
              properties:
                checkInterval:
                  description: CheckInterval describes the interval to trigger detection
                  format: int64
                  type: integer
                handleInterval:
                  description: HandleInterval describes the interval to handle conflicts
                    after detecting abnormal result
                  format: int64
                  type: integer
                metrics:
                  items:
                    type: string
                  type: array
                nodeSelector:
                  additionalProperties:
                    type: string
                  description: |-
                    INSERT ADDITIONAL SPEC FIELDS - desired state of cluster
                    Important: Run "make" to regenerate code after modifying this file
                  type: object
                recoverInterval:
                  description: RecoverInterval describes the interval to recover conflicts
                    after detecting normal result
                  format: int64
                  type: integer
                recoverRules:
                  items:
                    description: DetectActionRules define detectors and actions
                    properties:
                      actions:
                        items:
                          description: DetectAction define detector config
                          properties:
                            args:
                              description: |-
                                RawMessage is a raw encoded JSON value.
                                It implements Marshaler and Unmarshaler and can
                                be used to delay JSON decoding or precompute a JSON encoding.
                              format: byte
                              type: string
                            name:
                              type: string
                          required:
                            - args
                            - name
                          type: object
                        type: array
                      detects:
                        items:
                          description: DetectAction define detector config
                          properties:
                            args:
                              description: |-
                                RawMessage is a raw encoded JSON value.
                                It implements Marshaler and Unmarshaler and can
                                be used to delay JSON decoding or precompute a JSON encoding.
                              format: byte
                              type: string
                            name:
                              type: string
                          required:
                            - args
                            - name
                          type: object
                        type: array
                    required:
                      - actions
                      - detects
                    type: object
                  type: array
                rules:
                  items:
                    description: DetectActionRules define detectors and actions
                    properties:
                      actions:
                        items:
                          description: DetectAction define detector config
                          properties:
                            args:
                              description: |-
                                RawMessage is a raw encoded JSON value.
                                It implements Marshaler and Unmarshaler and can
                                be used to delay JSON decoding or precompute a JSON encoding.
                              format: byte
                              type: string
                            name:
                              type: string
                          required:
                            - args
                            - name
                          type: object
                        type: array
                      detects:
                        items:
                          description: DetectAction define detector config
                          properties:
                            args:
                              description: |-
                                RawMessage is a raw encoded JSON value.
                                It implements Marshaler and Unmarshaler and can
                                be used to delay JSON decoding or precompute a JSON encoding.
                              format: byte
                              type: string
                            name:
                              type: string
                          required:
                            - args
                            - name
                          type: object
                        type: array
                    required:
                      - actions
                      - detects
                    type: object
                  type: array
                type:
                  enum:
                    - app
                    - node
                    - container
                  type: string
              required:
                - checkInterval
                - handleInterval
                - metrics
                - nodeSelector
                - recoverInterval
                - rules
                - type
              type: object
            status:
              description: RuleCheckStatus defines the observed state of RuleCheck
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
